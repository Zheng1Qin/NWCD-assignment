CREATE TABLE ods_crm_owner (
    owner_territory_id  INTEGER PRIMARY KEY,
    owner_name          VARCHAR(255),
    owner_type          VARCHAR(100),
    owner_team          VARCHAR(100),
    owner_manager       VARCHAR(255)
);
